files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      0 0 * * 1,3,5 root /var/app/current/npm run scrape-categories

commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/*.bak"

